server:
  port: 9007
spring:
  application:
    name: feign-server
# 启动feign的容错机制
feign:
 hystrix:
  enabled: true
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://localhost:8761/eureka